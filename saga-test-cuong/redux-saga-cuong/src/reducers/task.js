const oneInitialState = {
    listTask:[]
}
const taskReducer = (state = oneInitialState, action) => {
    switch (action.type) {
        // case ACTION_TYPE_1:
        //     return state
        default:
            return state
    }
}
export default taskReducer;